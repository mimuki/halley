#!/usr/bin/env rc


awk 'BEGIN {RS="--------------\n"} /status id: '$1'/ {print}' $HOME/.config/msync/msync_accounts/*/home.list \
	| fold -s -w 120 \
	| awk '
		/^status id: / 	{next} \
		/^url: / 	{next} \
		/^boosted by: / {sub(/^boosted by: /, "△ "); print; next} \
		/^reply to: / 	{next} \
		/^boost of: /	{next} \
		/^[0-9]+favs.*[0-9]+boosts.*[0-9]+replies$/ {next} \
		{print} \
	' \
	| ./color.awk
